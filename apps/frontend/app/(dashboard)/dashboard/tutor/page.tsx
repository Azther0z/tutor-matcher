import { RoutePlaceholder } from "@/src/components/route-placeholder";

export default function TutorDashboardPage() {
  return (
    <RoutePlaceholder
      title="Tutor dashboard"
      description="Tutor statistics and engagement metrics will be displayed here."
      backlogIds={["PROF-5"]}
    />
  );
}
