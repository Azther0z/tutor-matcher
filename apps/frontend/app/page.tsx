import { RoutePlaceholder } from "@/src/components/route-placeholder";

export default function Home() {
  return (
    <RoutePlaceholder
      title="Homepage"
      description="Find the right tutor, book a lesson, and learn with confidence."
    />
  );
}
