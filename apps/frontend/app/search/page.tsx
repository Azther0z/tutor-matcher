import { RoutePlaceholder } from "@/src/components/route-placeholder";

export default function SearchPage() {
  return (
    <RoutePlaceholder
      title="Find a tutor"
      description="Tutor search, filters, and ranked recommendations will be implemented here."
      backlogIds={["DISC-1", "DISC-2"]}
    />
  );
}
