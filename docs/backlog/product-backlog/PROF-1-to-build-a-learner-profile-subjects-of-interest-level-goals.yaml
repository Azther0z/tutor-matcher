# Cancelled: docs/backlog/reconciliation.md marks this ⛔. No learner profile (subjects of
# interest, level, goals) exists in the product — student-side settings are account,
# notifications, and wallet only (docs/user-journeys.md). Removed from sprint-1's
# story_ids and execution fields; the file stays per the backlog contract.
#
# Unresolved: DISC-2 depends on PROF-1 for its personalization input and its "recommended"
# ranking basis. That dependency is left unchanged here — replacing it is a product
# decision this reconciliation pass does not make (see docs/backlog/reconciliation.md and
# issue #63, PROF-1 row).
id: PROF-1
title: "To build a student profile (subjects of interest, level, goals)"
description: "As a Student, I want to build a student profile (subjects of interest, level, goals) so that I get better tutor recommendations."
epic: "User Profile Management"
role: "Student"
lifecycle: cancelled
story_points: 2
dependencies: ["AUTH-1"]
acceptance_criteria:
  - given: "a Student is on the profile setup page,"
    when: "they save subjects, level, AND goals,"
    then: "the profile is stored and used for search ranking."
  - given: "a Student is on the profile setup page,"
    when: "they save without providing subjects, level, OR goals,"
    then: "the profile is not stored and the missing fields are highlighted."

sources:
  - file: ../../sources/Product Backlog v1.2.html
    locator: "User Profile Management / US2-1"
journey:
  - route: "/settings"
    action: "Update subject"
